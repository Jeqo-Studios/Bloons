package net.jeqo.bloons.balloon;

/**
 * An example enum to represent different balloon example file types
 */
public enum ExampleBalloonType {
    /**
     * Used for balloons that use regular Minecraft models
     */
    REGULAR,
    /**
     * Used for balloons that use ME models
     */
    MEG,
}
